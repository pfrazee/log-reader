"File path duck-typing"

This app will use pathnames to infer content-types, and schemas, of data-files stored in dats. It will assume that files which match against '/log/*.txt' are text/plain files which should be listed in this application's feed.